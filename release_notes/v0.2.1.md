- Version footer with a check-for-updates button — apply updates from the window
- Active-session prompt lines expand with the accordion (500-char excerpt)
- i18n extracted to single-source i18n/*.json shared by daemon and window
- Session/event lists render with keyed diffing — no more flicker on poll
- Sessions and events split by Claude/Codex tab; proper empty-state copy
- Session rows show the real session title; injected IDE XML stripped from prompts
- Chip hover/click isolated from row (chip opens folder, row focuses window)
- Fixed events-card header height jump when switching sub-tabs
