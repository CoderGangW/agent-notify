- **Fix: tray left-click did nothing on recent macOS** — the status-item click reached Wails with an unexpected event type and was dropped; patched via vendored build
- Signed-in Claude account email shown on the plan-limits card
- Login autostart toggle in settings
- Any tray click (left/right/double) opens the window
