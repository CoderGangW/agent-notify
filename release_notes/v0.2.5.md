- **Security: updates never auto-install** — background checks only notify; installing takes an explicit click in the footer
- Login item shows the app name/icon (launch agent runs the app-bundle binary)
- Window reopens automatically after an in-window update
- GitHub button in the footer; theme-aware tooltips; pin tooltip i18n fix
